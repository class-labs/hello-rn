import { SafeAreaView } from "react-native";

import { SignupForm } from "./SignupForm";

export function App() {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <SignupForm />
    </SafeAreaView>
  );
}
